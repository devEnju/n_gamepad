name: n_gamepad_example
description: Demonstrates how to use the n_gamepad plugin.
publish_to: 'none'

environment:
  sdk: '^3.12.0'

dependencies:
  flutter:
    sdk: flutter

  n_gamepad:
    path: ../

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
